Minimal uwsgi app
-----------------
A hello world style flask app for use testing ansible playbooks

`Master` branch returns `{"msg": "hello world" }` JSON response.

`v2` branch returns `{"msg": "new hello world" }`.
