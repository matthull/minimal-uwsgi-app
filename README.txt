Minimal uwsgi app
-----------------
A hello world style flask app for use testing ansible playbooks

`Master` branch returns `hello world again` `text/plain` response.

`v2` branch returns `hello world again`.
